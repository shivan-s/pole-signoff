name: Lint

on:
  push:
  pull_request:
    branches: [main]

jobs:
  format:
    name: Format
    uses: ./.github/workflows/ci-build.yml
    with:
      run: |
        npm run format
  lint:
    name: Format
    uses: ./.github/workflows/ci-build.yml
    with:
      run: |
        npm run lint
  check:
    name: Format
    uses: ./.github/workflows/ci-build.yml
    with:
      run: |
        npm run check
